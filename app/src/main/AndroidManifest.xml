<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.pouillos.mysosapp">

    <uses-permission android:name="android.permission.SEND_SMS"/>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature android:name="android.hardware.camera" />
    <uses-permission android:name="android.permission.FLASHLIGHT"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activities.AccueilActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.afficher.AfficherUtilisateurEtapeUnActivity"
            android:windowSoftInputMode="stateHidden"/>
        <activity android:name=".activities.afficher.AfficherUtilisateurEtapeDeuxActivity"
            android:windowSoftInputMode="stateHidden"/>
        <activity android:name=".activities.afficher.AfficherUtilisateurEtapeTroisActivity"
            android:windowSoftInputMode="stateHidden"/>
        <activity android:name=".activities.afficher.AfficherContactActivity"
            android:windowSoftInputMode="stateHidden"/>
        <activity android:name=".activities.afficher.AfficherListeContactActivity"
            android:windowSoftInputMode="stateHidden"/>
        <activity android:name=".activities.afficher.AfficherSmsPersoActivity"
            android:windowSoftInputMode="stateHidden"/>

        <activity android:name=".activities.action.LampeActivity"
            android:windowSoftInputMode="stateHidden"/>
    </application>

</manifest>